const weidth = prompt("Enter the weidth of the rectangle");
const height = prompt("Enter the height of the rectangle");

const area = weidth * height;
console.log(area);
alert(`The area of the rectangle is ${area}`);

document.write(`The area of the rectangle is ${area}`);